#pragma once

#include <raylib.h>

/*
class GameCamera{
    // raylib has a Camera2D, but I want to try my own

    Vector2 pos;
    Vector2 size;
};
*/

typedef struct GameCamera{
    Vector2 pos;
    Vector2 size;
}GameCamera;
